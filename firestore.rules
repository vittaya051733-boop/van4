rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Start secure by default. Update this for app-specific access rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
